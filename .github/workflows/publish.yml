name: Build and Publish

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      publish_to_pypi:
        description: 'Publish to PyPI'
        required: true
        default: 'false'
        type: boolean

jobs:
  build-wheels:
    name: Build wheel (${{ matrix.os }}, Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # Note: windows-11-arm excluded until pyarrow publishes Windows ARM64 wheels
        os: [ubuntu-latest, ubuntu-24.04-arm, macos-15-intel, macos-latest, windows-latest]
        python-version: ['3.10', '3.11', '3.12', '3.13', '3.14']

    steps:
    - uses: actions/checkout@v4
      with:
        lfs: true

    - name: Install Rust toolchain
      uses: dtolnay/rust-toolchain@stable

    - name: Install uv
      uses: astral-sh/setup-uv@v4

    - name: Build wheel
      run: uvx cibuildwheel==3.3.1 --output-dir wheelhouse
      env:
        CIBW_BUILD: "cp${{ matrix.python-version == '3.10' && '310' || matrix.python-version == '3.11' && '311' || matrix.python-version == '3.12' && '312' || matrix.python-version == '3.13' && '313' || '314' }}-*"
        CIBW_BUILD_VERBOSITY: 1
        CIBW_BEFORE_BUILD: "pip install numpy cython"
        # Install Rust inside manylinux container (host toolchain not available)
        CIBW_BEFORE_ALL_LINUX: "curl https://sh.rustup.rs -sSf | sh -s -- --default-toolchain stable -y"
        CIBW_ENVIRONMENT_LINUX: 'PATH="$HOME/.cargo/bin:$PATH"'
        # Build only native arch per runner (no cross-compilation or emulation)
        CIBW_ARCHS_LINUX: "native"
        CIBW_ARCHS_MACOS: "native"
        # Build only native arch per runner (no cross-compilation)
        CIBW_ARCHS_WINDOWS: "native"

    - name: Upload wheel
      uses: actions/upload-artifact@v4
      with:
        name: wheel-${{ matrix.os }}-${{ matrix.python-version }}
        path: ./wheelhouse/*.whl

  build-sdist:
    name: Build source distribution
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        lfs: true

    - name: Install uv
      uses: astral-sh/setup-uv@v4

    - name: Build sdist
      run: uv build --sdist

    - name: Upload sdist
      uses: actions/upload-artifact@v4
      with:
        name: sdist
        path: dist/*.tar.gz

  test-wheels:
    name: Test wheel (${{ matrix.os }}, Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    needs: build-wheels
    strategy:
      fail-fast: false
      matrix:
        # Note: windows-11-arm excluded until pyarrow publishes Windows ARM64 wheels
        os: [ubuntu-latest, ubuntu-24.04-arm, macos-15-intel, macos-latest, windows-latest]
        python-version: ['3.10', '3.11', '3.12', '3.13', '3.14']

    steps:
    - uses: actions/checkout@v4
      with:
        lfs: true

    - name: Install uv
      uses: astral-sh/setup-uv@v4

    - name: Download wheel artifact
      uses: actions/download-artifact@v4
      with:
        name: wheel-${{ matrix.os }}-${{ matrix.python-version }}
        path: dist/

    - name: Create venv and install wheel (Unix)
      if: runner.os != 'Windows'
      run: |
        uv venv .venv --python ${{ matrix.python-version }}
        WHEEL=$(ls dist/*.whl | head -1)
        uv pip install --python .venv "${WHEEL}[test]"

    - name: Create venv and install wheel (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        uv venv .venv --python ${{ matrix.python-version }}
        $wheel = (Get-ChildItem dist/*.whl | Select-Object -First 1).FullName
        uv pip install --python .venv "${wheel}[test]"

    - name: Run tests
      run: uv run --python .venv pytest tests/ -v --tb=short

    - name: Run tests (Rust backend)
      run: uv run --python .venv pytest tests/ -v --tb=short
      env:
        LIBXRK_BACKEND: rust

  pyodide:
    uses: ./.github/workflows/pyodide.yml

  upload-pyodide:
    name: Upload Pyodide wheels to GitHub Release
    runs-on: ubuntu-latest
    needs: [pyodide]
    if: github.event_name == 'release'
    permissions:
      contents: write

    steps:
    # Only the legacy-tagged (pyodide_*) wheel is hosted here. PEP 783
    # (pyemscripten_*) wheels go to PyPI in the publish job instead.
    - name: Download Pyodide wheels (legacy tag)
      uses: actions/download-artifact@v4
      with:
        pattern: pyodide-wheel-0.*
        path: dist/
        merge-multiple: true

    - name: List wheels
      run: ls -la dist/

    - name: Upload to GitHub Release
      env:
        GH_TOKEN: ${{ github.token }}
      run: gh release upload "${{ github.event.release.tag_name }}" dist/* --clobber --repo "${{ github.repository }}"

  publish:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    needs: [build-wheels, build-sdist, test-wheels, pyodide]
    if: github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && inputs.publish_to_pypi == true)
    environment:
      name: pypi
      url: https://pypi.org/p/libxrk

    permissions:
      id-token: write  # Required for trusted publishing
      contents: write  # Required for uploading release assets

    steps:
    - name: Download CPython wheels
      uses: actions/download-artifact@v4
      with:
        pattern: wheel-*
        path: dist/
        merge-multiple: true

    - name: Download sdist
      uses: actions/download-artifact@v4
      with:
        name: sdist
        path: dist/

    # PEP 783 wasm wheels (pyemscripten_*) are accepted by PyPI. Pyodide 0.29
    # and earlier predate the PEP, so their pyodide_*-tagged wheels are NOT
    # collected here — they ship as GitHub Release assets.
    - name: Download PEP 783 Pyodide wheel
      uses: actions/download-artifact@v4
      with:
        name: pyodide-wheel-314
        path: dist/

    - name: List artifacts
      run: ls -la dist/

    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        packages-dir: dist/

    - name: Upload assets to GitHub Release
      if: github.event_name == 'release'
      env:
        GH_TOKEN: ${{ github.token }}
      run: gh release upload "${{ github.event.release.tag_name }}" dist/* --repo "${{ github.repository }}"
